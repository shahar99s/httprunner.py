[pytest]
addopts = -s
# Collect HttpRunner workflow classes in addition to the default Test* classes
python_classes = Test* Workflow*
# https://docs.pytest.org/en/latest/how-to/output.html
junit_logging = all
junit_duration_report = total
log_cli = False
